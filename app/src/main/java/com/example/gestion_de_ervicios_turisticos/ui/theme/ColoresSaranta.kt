package com.example.gestion_de_ervicios_turisticos.ui.theme

import androidx.compose.ui.graphics.Color

// Paleta oficial de la marca Saranta — usar estos colores en vez de valores
// hardcodeados (Color(0xFF...)) directamente en cada pantalla.
object ColoresSaranta {
    val VerdeOscuro = Color(0xFF0D5B54)
    val Verde = Color(0xFF1BA795)
    val AzulOscuro = Color(0xFF134E6F)
    val Dorado = Color(0xFFD4A017)
    val Crema = Color(0xFFF2E5C6)
    val Negro = Color(0xFF1E272D)
}